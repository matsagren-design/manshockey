import React,{useMemo}from'react';import{Bot,CalendarDays,ExternalLink,FileText,MapPin,Newspaper,Plane,Target,Trophy,Video}from'lucide-react';import{Page}from'../components/Layout.jsx';import{formatDate,isSameMatch}from'../lib/api.js';function Section({title,icon,children}){return <section className="mc-section"><h2>{icon}{title}</h2>{children}</section>}export function MatchCenter({matches,scout,media,travel,documents,selectedMatchId,setSelectedMatchId}){const selected=useMemo(()=>matches.find(m=>String(m.id)===String(selectedMatchId))||matches[0],[matches,selectedMatchId]);if(!selected)return <Page kicker="Matchcenter" title="Ingen match"><p>Lägg till en match först.</p></Page>;const matchScout=scout.filter(x=>isSameMatch(x,selected.id));const matchMedia=media.filter(x=>isSameMatch(x,selected.id));const matchTravel=travel.filter(x=>isSameMatch(x,selected.id));const matchDocs=documents.filter(x=>isSameMatch(x,selected.id));return <Page kicker="Matchcenter 2.0" title={`Brooks ${selected.home_away==='Hemma'?'vs':'@'} ${selected.opponent}`} action={<select className="match-select" value={selected.id} onChange={e=>setSelectedMatchId(e.target.value)}>{matches.map(m=><option key={m.id} value={m.id}>{formatDate(m.game_date)} – {m.opponent}</option>)}</select>}><div className="match-layout"><div className="match-main"><div className="match-hero"><span className="pill">{selected.game_status||selected.home_away}</span><h2>{selected.opponent}</h2><p><CalendarDays size={16}/> {formatDate(selected.game_date)}</p><p><MapPin size={16}/> {selected.arena} · {selected.city||'Brooks'}</p><div className="match-actions">{selected.tv_link&&<a href={selected.tv_link} target="_blank"><Video size={16}/> TV / FloHockey</a>}{selected.map_url&&<a href={selected.map_url} target="_blank"><MapPin size={16}/> Karta</a>}</div></div><Section title="Pre Game" icon={<FileText/>}><div className="report-grid"><div><h3>Arena/Väder</h3><p>{selected.weather_note||'Väder saknas.'}</p></div><div><h3>Lineup/Special Teams</h3><p>{selected.lineup_note||'Lineup saknas.'}<br/>{selected.special_teams||''}</p></div><div><h3>Motståndarnas form</h3><p>{selected.opponent_form||'Form saknas.'}</p></div><div><h3>Domare</h3><p>{selected.referee_note||'Domarinfo saknas.'}</p></div></div></Section><Section title="AI Matchanalys" icon={<Bot/>}><p>{selected.ai_summary||'AI-sammanfattning skapas utifrån rapport, scout, media och matchdata.'}</p></Section><Section title="Scout" icon={<Target/>}><div className="mini-grid">{matchScout.length?matchScout.map(s=><article className="mini-card" key={s.id}><b>{s.category}</b><strong>{s.score}</strong><p>{s.note}</p><small>{s.ai_comment}</small></article>):<p>Ingen scout kopplad.</p>}</div></Section><Section title="Media" icon={<Newspaper/>}><div className="mini-grid">{matchMedia.length?matchMedia.map(m=><a className="mini-card" href={m.url} target="_blank" key={m.id}><b>{m.title}</b><span>{m.media_type||m.tag}</span><p>{m.summary}</p><ExternalLink size={14}/></a>):<p>Ingen media kopplad.</p>}</div></Section></div><aside className="match-side"><Section title="Resultat" icon={<Trophy/>}><p>{selected.result||'Resultat saknas'}</p><div className="score-row"><strong>{selected.brooks_goals??'-'}</strong><span>Brooks</span><strong>{selected.opponent_goals??'-'}</strong><span>{selected.opponent}</span></div></Section><Section title="Resor" icon={<Plane/>}>{matchTravel.length?matchTravel.map(t=><p key={t.id}><b>{t.airline}</b>: {t.origin} → {t.destination}, {t.status}</p>):<p>Ingen resa kopplad.</p>}</Section><Section title="Dokument" icon={<FileText/>}>{matchDocs.length?matchDocs.map(d=><p key={d.id}><b>{d.title}</b> · {d.status}</p>):<p>Inga dokument kopplade.</p>}</Section></aside></div></Page>}