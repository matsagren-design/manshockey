import React,{useMemo,useState}from'react';
import{Bot,CalendarDays,ExternalLink,FileText,MapPin,Newspaper,Plane,Save,Target,Trophy,Video}from'lucide-react';
import{Page}from'../components/Layout.jsx';
import{formatDate,isSameMatch,updateItem}from'../lib/api.js';
function Section({title,icon,children}){return <section className="mc-section"><h2>{icon}{title}</h2>{children}</section>}
export function MatchCenter({matches,scout,media,travel,documents,selectedMatchId,setSelectedMatchId,user,reload}){const selected=useMemo(()=>matches.find(m=>String(m.id)===String(selectedMatchId))||matches[0],[matches,selectedMatchId]);const matchScout=scout.filter(x=>isSameMatch(x,selected?.id));const matchMedia=media.filter(x=>isSameMatch(x,selected?.id));const matchTravel=travel.filter(x=>isSameMatch(x,selected?.id));const matchDocs=documents.filter(x=>isSameMatch(x,selected?.id));const[draft,setDraft]=useState(selected||{});const[message,setMessage]=useState('');React.useEffect(()=>setDraft(selected||{}),[selected?.id]);async function saveReport(){const res=await updateItem('matches',draft);setMessage(res.ok?'Matchcenter uppdaterat.':(res.error||'Kunde inte spara.'));setTimeout(()=>reload?.(),700)}if(!selected)return <Page kicker="Matchcenter" title="Ingen match"><p>Lägg till en match först.</p></Page>;return <Page kicker="Enterprise Matchcenter" title={`Brooks ${selected.home_away==='Hemma'?'vs':'@'} ${selected.opponent}`} action={<select className="match-select" value={selected.id} onChange={e=>setSelectedMatchId(e.target.value)}>{matches.map(m=><option key={m.id} value={m.id}>{formatDate(m.game_date)} – {m.opponent}</option>)}</select>}>{message&&<div className="notice">{message}</div>}<div className="match-layout"><div className="match-main"><div className="match-hero"><span className="pill">{selected.game_status||selected.home_away}</span><h2>{selected.opponent}</h2><p><CalendarDays size={16}/> {formatDate(selected.game_date)}</p><p><MapPin size={16}/> {selected.arena} · {selected.city||'Brooks'}</p><div className="match-actions">{selected.tv_link&&<a href={selected.tv_link} target="_blank" rel="noreferrer"><Video size={16}/> TV / FloHockey</a>}{selected.map_url&&<a href={selected.map_url} target="_blank" rel="noreferrer"><MapPin size={16}/> Karta</a>}</div></div><Section title="Rapporter" icon={<FileText/>}><div className="report-grid"><div><h3>Inför match</h3><p>{selected.report_before||'Ingen inför-rapport ännu.'}</p></div><div><h3>Efter match</h3><p>{selected.report_after||'Efterrapport fylls i efter match.'}</p></div></div></Section><Section title="AI Matchanalys" icon={<Bot/>}><p>{selected.ai_summary||'AI-sammanfattning skapas utifrån rapport, scout, media och resa.'}</p></Section><Section title="Scout" icon={<Target/>}><div className="mini-grid">{matchScout.length?matchScout.map(s=><article className="mini-card" key={s.id}><b>{s.category}</b><strong>{s.score}</strong><p>{s.note}</p><small>{s.ai_comment}</small></article>):<p>Ingen scout kopplad till matchen ännu.</p>}</div></Section><Section title="Media" icon={<Newspaper/>}><div className="mini-grid">{matchMedia.length?matchMedia.map(m=><a className="mini-card" href={m.url} target="_blank" key={m.id}><b>{m.title}</b><span>{m.media_type||m.tag}</span><p>{m.summary}</p><ExternalLink size={14}/></a>):<p>Ingen media kopplad till matchen ännu.</p>}</div></Section></div><aside className="match-side"><Section title="Resultat" icon={<Trophy/>}><p>{selected.result||'Resultat saknas'}</p><div className="score-row"><strong>{selected.brooks_goals??'-'}</strong><span>Brooks</span><strong>{selected.opponent_goals??'-'}</strong><span>{selected.opponent}</span></div></Section><Section title="Resor" icon={<Plane/>}>{matchTravel.length?matchTravel.map(t=><p key={t.id}><b>{t.airline}</b>: {t.origin} → {t.destination}, {t.status}</p>):<p>Ingen resa kopplad.</p>}</Section><Section title="Dokument" icon={<FileText/>}>{matchDocs.length?matchDocs.map(d=><p key={d.id}><b>{d.title}</b> · {d.status}</p>):<p>Inga dokument kopplade.</p>}</Section>{user?.role==='admin'&&<Section title="Snabbredigera" icon={<Save/>}>{['game_status','result','brooks_goals','opponent_goals','report_before','report_after','ai_summary'].map(f=><label key={f}>{f}<textarea value={draft[f]||''} onChange={e=>setDraft({...draft,[f]:e.target.value})}/></label>)}<button onClick={saveReport}><Save size={16}/> Spara match</button></Section>}</aside></div></Page>}
