import React,{useState}from'react';import{Send,Sparkles}from'lucide-react';import{Page}from'../components/Layout.jsx';export function AICoach(){const[question,setQuestion]=useState('Ge mig veckans fokus för Måns baserat på match, statistik och scout.');const[answer,setAnswer]=useState('');async function ask(){const r=await fetch('/api/ai-coach',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question})});const j=await r.json();setAnswer(j.answer||'Inget svar.')}return <Page kicker="AI Coach Pro" title="Personlig hockeyanalys"><div className="ai"><Sparkles/><p>AI Coach läser matcher, GameCenter, spelarstatistik och scout från D1.</p><div className="prompt-grid"><button onClick={()=>setQuestion('Sammanfatta senaste matchen.')}>Senaste match</button><button onClick={()=>setQuestion('Ge tre styrkor och tre utvecklingsområden.')}>Styrkor/svagheter</button><button onClick={()=>setQuestion('Skapa träningsfokus för kommande vecka.')}>Träningsfokus</button></div><textarea value={question} onChange={e=>setQuestion(e.target.value)}/><button onClick={ask}><Send size={16}/> Fråga</button>{answer&&<div className="answer">{answer}</div>}</div></Page>}