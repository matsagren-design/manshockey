import React,{useMemo,useState}from'react';import{Activity,Plus,Save,Trash2,Trophy}from'lucide-react';import{Page}from'../components/Layout.jsx';import{createItem,deleteItem,formatDate,isSameMatch,updateItem}from'../lib/api.js';export function GameCenter({matches,gameEvents,playerStats,selectedMatchId,setSelectedMatchId,user,reload}){const selected=useMemo(()=>matches.find(m=>String(m.id)===String(selectedMatchId))||matches[0],[matches,selectedMatchId]);const events=gameEvents.filter(e=>isSameMatch(e,selected?.id));const stats=playerStats.find(s=>isSameMatch(s,selected?.id))||{};const[draft,setDraft]=useState(selected||{});const[eventForm,setEventForm]=useState({match_id:selected?.id||'',period:'1',game_time:'00:00',event_type:'Notering',team:'Brooks',player:'Måns',note:''});const[message,setMessage]=useState('');React.useEffect(()=>{setDraft(selected||{});setEventForm(f=>({...f,match_id:selected?.id||''}))},[selected?.id]);if(!selected)return <Page kicker="GameCenter" title="Ingen match"><p>Lägg till en match först.</p></Page>;const isAdmin=user?.role==='admin';async function saveMatch(){const res=await updateItem('matches',draft);setMessage(res.ok?'GameCenter sparat.':res.error||'Fel.');setTimeout(()=>reload?.(),700)}async function addEvent(e){e.preventDefault();const res=await createItem('game_events',eventForm);setMessage(res.ok?'Event sparad.':res.error||'Fel.');setEventForm({...eventForm,note:''});setTimeout(()=>reload?.(),700)}async function removeEvent(id){if(!confirm('Ta bort händelse?'))return;await deleteItem('game_events',id);reload?.()}return <Page kicker="Live GameCenter" title={`Brooks ${selected.home_away==='Hemma'?'vs':'@'} ${selected.opponent}`} action={<select className="match-select" value={selected.id} onChange={e=>setSelectedMatchId(e.target.value)}>{matches.map(m=><option key={m.id} value={m.id}>{formatDate(m.game_date)} – {m.opponent}</option>)}</select>}>{message&&<div className="notice">{message}</div>}<div className="game-scoreboard"><div><span>Brooks</span><strong>{selected.brooks_goals??'-'}</strong><small>{selected.brooks_shots??'-'} skott · PP {selected.brooks_pp||'-'}</small></div><div><span>{selected.period||'Ej startad'}</span><b>{selected.game_clock||'--:--'}</b><small>{selected.game_status||'Kommande'}</small></div><div><span>{selected.opponent}</span><strong>{selected.opponent_goals??'-'}</strong><small>{selected.opponent_shots??'-'} skott · PP {selected.opponent_pp||'-'}</small></div></div><div className="game-grid-4"><div><b>xG Brooks</b><strong>{selected.xg_brooks||'-'}</strong></div><div><b>xG Motståndare</b><strong>{selected.xg_opponent||'-'}</strong></div><div><b>Faceoffs</b><strong>{selected.brooks_faceoffs||'-'} / {selected.opponent_faceoffs||'-'}</strong></div><div><b>Momentum</b><p>{selected.momentum_note||'Ej angivet'}</p></div></div><div className="game-layout"><section className="mc-section"><h2><Activity/> Tidslinje</h2><div className="timeline">{events.length?events.map(ev=><div className="event-row" key={ev.id}><b>{ev.period} · {ev.game_time}</b><span>{ev.event_type}</span><p>{ev.team} · {ev.player} — {ev.note}</p>{isAdmin&&<button className="danger" onClick={()=>removeEvent(ev.id)}><Trash2 size={14}/></button>}</div>):<p>Inga händelser ännu.</p>}</div>{isAdmin&&<form className="inline-form" onSubmit={addEvent}>{['period','game_time','event_type','team','player','note'].map(f=><input key={f} placeholder={f} value={eventForm[f]} onChange={e=>setEventForm({...eventForm,[f]:e.target.value})}/>) }<button><Plus size={16}/> Lägg till</button></form>}</section><aside className="match-side">{isAdmin&&<section className="mc-section"><h2><Save/> Liveuppdatering</h2>{['game_status','period','game_clock','brooks_goals','opponent_goals','brooks_shots','opponent_shots','brooks_faceoffs','opponent_faceoffs','brooks_pp','opponent_pp','xg_brooks','xg_opponent','momentum_note'].map(f=><label key={f}>{f}<input value={draft[f]||''} onChange={e=>setDraft({...draft,[f]:e.target.value})}/></label>)}<button onClick={saveMatch}><Save size={16}/> Spara live</button></section>}<section className="mc-section"><h2><Trophy/> Måns statistik</h2><div className="stat-line"><span>G</span><b>{stats.goals||0}</b><span>A</span><b>{stats.assists||0}</b><span>P</span><b>{stats.points||0}</b></div><p>+/- {stats.plus_minus||0} · PIM {stats.pim||0} · Skott {stats.shots||0} · TOI {stats.toi||'—'}</p></section></aside></div></Page>}