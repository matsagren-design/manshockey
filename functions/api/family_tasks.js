function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8'}})}
async function readBody(request){try{return await request.json()}catch{return {}}}
function getCookie(request,name){const cookie=request.headers.get('Cookie')||'';for(const part of cookie.split(';').map(x=>x.trim())){const [k,...v]=part.split('=');if(k===name)return decodeURIComponent(v.join('='))}return null}
async function requireAdmin(context){const sid=getCookie(context.request,'mh_session');if(!sid||!context.env.DB)return null;const user=await context.env.DB.prepare('SELECT users.id,users.email,users.role FROM sessions JOIN users ON users.id=sessions.user_id WHERE sessions.id=? AND sessions.expires_at > datetime("now") LIMIT 1').bind(sid).first();return user?.role==='admin'?user:null}
const fallback=[{"id": 1, "title": "Kontrollera försäkring", "owner": "Familjen", "due_date": "", "status": "Öppen", "note": "BCHL + svensk försäkring."}];
export async function onRequestGet(context){try{if(!context.env.DB)return json({source:'fallback',items:fallback});const {results}=await context.env.DB.prepare('SELECT * FROM family_tasks ORDER BY id DESC LIMIT 500').all();return json({source:'d1',items:results})}catch(err){return json({source:'fallback-error',error:String(err),items:fallback})}}
export async function onRequestPost(context){const user=await requireAdmin(context);if(!user)return json({ok:false,error:'Unauthorized'},401);try{const body=await readBody(context.request);const result=await context.env.DB.prepare('INSERT INTO family_tasks (title, owner, due_date, status, note) VALUES (?, ?, ?, ?, ?)').bind(body.title ?? '', body.owner ?? '', body.due_date ?? '', body.status ?? '', body.note ?? '').run();return json({ok:true,action:'created',result})}catch(err){return json({ok:false,error:String(err)},500)}}
export async function onRequestPut(context){const user=await requireAdmin(context);if(!user)return json({ok:false,error:'Unauthorized'},401);try{const body=await readBody(context.request);if(!body.id)return json({ok:false,error:'Missing id'},400);const result=await context.env.DB.prepare('UPDATE family_tasks SET title=?, owner=?, due_date=?, status=?, note=?, updated_at=CURRENT_TIMESTAMP WHERE id=?').bind(body.title ?? '', body.owner ?? '', body.due_date ?? '', body.status ?? '', body.note ?? '', body.id).run();return json({ok:true,action:'updated',result})}catch(err){return json({ok:false,error:String(err)},500)}}
export async function onRequestDelete(context){const user=await requireAdmin(context);if(!user)return json({ok:false,error:'Unauthorized'},401);try{const url=new URL(context.request.url);const id=url.searchParams.get('id');if(!id)return json({ok:false,error:'Missing id'},400);const result=await context.env.DB.prepare('DELETE FROM family_tasks WHERE id=?').bind(id).run();return json({ok:true,action:'deleted',result})}catch(err){return json({ok:false,error:String(err)},500)}}