{
  "name": "manshockey",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=manshockey",
    "preview": "wrangler pages dev ."
  },
  "devDependencies": {
    "wrangler": "^4.108.0"
  }
}
